# DOCX QA Note

`LE_PSI_Paper_Support_Dossier.docx` was generated from
`PAPER_SUPPORT_DOSSIER.md` with Pandoc and an explicit Letter-size reference
DOCX.

Visual render QA could not be completed on this machine because LibreOffice
`soffice` is not installed. The Documents render tool was invoked and failed at
the conversion step with:

`FileNotFoundError: [Errno 2] No such file or directory: 'soffice'`

The Markdown file is the source of truth. The DOCX is a convenience copy for
sharing, not a manuscript.
